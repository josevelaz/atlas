/**
 * @deprecated Import from `./schema/index.ts` directly.
 *
 * This shim re-exports the schema barrel so any existing imports of
 * `./schema.ts` continue to resolve without changes.  It will be removed
 * once all consumers have been updated.
 */
export * from "./schema/index.ts";
