# Categories

## MVP

The MVP uses fixed Hey-style categories only:

- Inbox
- Feed
- Paper Trail

Users cannot create custom top-level categories in the MVP. This keeps the product opinionated, keeps routing behavior understandable, and keeps AI categorization constrained.

## Future

Future versions may support secondary tags or labels for power users, but they should not undermine the primary category model.
