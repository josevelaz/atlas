# Use need-based AI processing

The AI Assistant processes email content only when needed for enabled features rather than analyzing the user's entire mailbox by default. Categorization may run at ingestion, priority scoring may run for Imbox threads, and summaries or action items may run when a thread is viewed or requested. This balances the product's AI value with a stronger privacy promise for sensitive email data.
